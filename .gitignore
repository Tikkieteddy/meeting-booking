# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
/coverage

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (ห้าม commit ค่าจริงเด็ดขาด)
.env
.env*.local
.env.development
.env.staging
.env.production
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# tests
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# Playwright session ที่บันทึกไว้ตอนรันเทสต์ (มี cookie จึงห้าม commit)
/playwright/.auth/

# รายงาน Lighthouse ที่รันในเครื่อง — ไม่ต้องเก็บเข้า repo
/lighthouse-report.html

# ไฟล์ SQL ที่สร้างขึ้นเพื่อเอาไปวางใน SQL Editor — สร้างใหม่ได้เสมอด้วย npm run db:bootstrap-sql
/bootstrap.sql
/check-database.sql
