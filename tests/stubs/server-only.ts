/**
 * Stub ของแพ็กเกจ server-only สำหรับเทสต์
 * ในโปรดักชัน แพ็กเกจนี้จะ throw ถ้าถูก import จาก Client Component
 * ซึ่งเป็นการป้องกันที่เราต้องการ แต่ใน vitest ไม่มี React Server Component
 * จึงต้อง alias เป็นไฟล์ว่างเพื่อให้ทดสอบโค้ดฝั่ง server ได้
 */
export {};
